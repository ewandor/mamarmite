<div class="banner" id="banner-show" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%),
url('<%= cl_image_path @cook.location_photos.first[:public_id], crop: :fit %>')">

  <div class="card-cooker">

    <div class="card-cooker-img">
      <%= cl_image_tag(@cook.cooker_photo.path, crop: :fill, class: 'cooker-img-show') %>
    </div>

    <div class="card-cooker-id">
      <h3><%= @cook.nickname %> , <%= @cook.age %></h3>
      <h4>Speciality : <b><%= @cook.speciality %></b></h4>
      <h4> <em>"<%= @cook.motto %>"</em></h4>
    </div>

  </div>

<!-- slider-1 -->
<!-- <button type="button" class="btn" data-toggle="modal" data-target="#myModal" data-local="#myCarousel">Demo</button> -->
 <!-- slider-2 -->
 <button type="button" class="btn btn-primary btn-photos-show" data-toggle="modal" data-target="#myModal">
  Launch demo modal
</button>
<!-- <ul class="nav nav-pills nav-stacked">
  <li><a href="#lightbox" data-toggle="modal">Open Lightbox</a></li>
</ul> -->
 <%= render "shared/slider2" %>

</div>

<div class="summary">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-8">
        <div class="tabs-header-show">
          <%= link_to "Présentation", "#summary-icons", class: "header-summary-show" %>
          <%= link_to "Commentaires", "#", class: "header-summary-show" %>
          <%= link_to "Emplacement", "#map-show", class: "header-summary-show" %>
        </div>
      </div>
      <div class="col-xs-12 col-sm-4">
        <div id="book-form">
          <div id="book-form-header">
            <div> <%= @cook.price %> Euros </div>
            <div> par Marmitte </div>
          </div>
          <%= simple_form_for([@cook, @booking]) do |f| %>
            <%= f.input :foodies_number %>
            <%= f.input :starts_at, as: :string, input_html: { class: 'datepickerrr'} %>
            <%= f.button :submit %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-8">
        <div id="summary-icons">
          <div class="container">
            <div class="row">
              <div class="col-xs-2 text-center">
                <i class="fa fa-home" aria-hidden="true"></i>
              </div>
              <div class="col-xs-2 text-center">
                <i class="fa fa-users" aria-hidden="true"></i>
              </div>
            </div>
            <div class="row">
              <div class="col-xs-2 text-center">
                <b><%= @cook.home_style %></b>
              </div>
              <div class="col-xs-2 text-center">
                <b><%= @cook.capacity %> marmittes</b>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>


<div class="reviews">
</div>

<!--   <h4>Address: <b><%= @cook.address %></b></h4> -->
<div class="col-xs-8 col-xs-offset-2 text-center">
  <div id="map-show"  style="width: 100%; height: 600px;"></div>
</div>

<% content_for(:after_js) do %>
  <%= javascript_tag do %>
    $(document).ready(function() {
      $('.datepickerrr').datepicker({
       format: "dd/mm/yyyy",
       weekStart: 1,
       todayBtn: "linked",
       language: "fr",
       todayHighlight: true
    });
      var handler = Gmaps.build('Google');
      handler.buildMap({ internal: { id: 'map-show' } }, function() {
        markers = handler.addMarkers(<%= raw @cook_hash.to_json %>);
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        if (markers.length == 0) {
          handler.getMap().setZoom(2);
        } else if (markers.length == 1) {
          handler.getMap().setZoom(14);
        }
      });

      $('.modal-body').slick({
        setting-name: setting-value
      });

    });
  <% end %>
<% end %>





  <!-- a ajouter slide show sur le bouton link -->
  <% @cook.recipe_photos.each do |photo| %>
    <div>
      <%= cl_image_tag photo.path, width: 200, height: 300, crop: :fill %>
    </div>
  <% end %>
  <% @cook.location_photos.each do |photo| %>
    <div>
       <%= cl_image_tag photo.path, width: 200, height: 300, crop: :fill %>
    </div>
  <% end %>
  </div>






